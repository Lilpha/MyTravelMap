# MyTravelMap 서버 설정 가이드

Express.js 백엔드 서버 실행 방법입니다.

## 📦 설치

```powershell
# 1. 프로젝트 폴더로 이동
cd c:\Users\kim\Desktop\MyTravelMap

# 2. Node.js 패키지 설치
npm install
```

## 🔑 Google Gemini API 키 설정 (선택사항 - AI 제목 생성 기능)

### 1. Google Gemini API 키 발급
- [Google AI Studio](https://aistudio.google.com/app/apikeys)에 접속
- API 키 생성
- `.env` 파일 생성 (또는 `.env.example` 복사):

```powershell
# .env 파일 생성
copy .env.example .env
```

### 2. `.env` 파일에 API 키 입력

```
GEMINI_API_KEY=AIzaSy...your-gemini-api-key...
```

### 3. API 키 없이도 기본 동작
- API 키가 없으면 템플릿 기반 제목 생성 로직만 동작
- 사용자가 수동으로 제목 입력 가능

## ▶️ 실행

```powershell
npm start
```

또는

```powershell
node server.js
```

## 🌐 접속

브라우저에서 다음 주소로 접속하세요:
- **http://localhost:3000**

## 📁 폴더 구조

```
MyTravelMap/
├── server.js              # Express 백엔드
├── package.json           # 의존성 관리
├── .env                   # 환경변수 (API 키)
├── .env.example           # 환경변수 예제
├── views/                 # EJS 템플릿 파일
│   ├── index.ejs         # 메인 페이지 (여행 목록)
│   ├── add.ejs           # 여행 추가 페이지 (4단계)
│   ├── travel-detail.ejs # 여행 상세 페이지
│   └── 404.ejs           # 404 오류 페이지
├── public/               # 정적 파일
│   ├── css/
│   │   └── style.css     # 공통 스타일
│   ├── js/
│   │   ├── main.js       # 공통 로직
│   │   ├── add.js        # 기존 여행 추가 로직
│   │   └── travel-add.js # 새로운 4단계 여행 추가
│   └── uploads/          # 업로드된 이미지/영상 저장
└── data/
    └── travels.json      # 여행 데이터 저장소
```

## ✨ 새로운 기능: 4단계 여행 추가

### Step 1: 위치 선택 📍
- 네이버 지도에서 여행지 위치 선택
- 지도 클릭으로 위도/경도 자동 입력

### Step 2: 사진 업로드 🎬
- 여행지에서 촬영한 모든 사진/영상 업로드
- 드래그 & 드롭 지원
- EXIF 데이터에서 자동으로 GPS 정보 추출

### Step 3: AI 제목 생성 ✨
- Claude AI가 위치 + 사진 개수를 분석
- 자동으로 여행 제목 제안
- 추천 제목 중 선택 또는 수동 입력 가능

### Step 4: 저장 ✓
- 모든 정보 확인 후 저장
- 저장 완료 시 여행 상세 페이지로 이동

## 🔗 라우팅

| 경로 | 메서드 | 설명 |
|------|--------|------|
| `/` | GET | 메인 페이지 (여행 목록) |
| `/add` | GET | 여행 추가 페이지 (4단계 UI) |
| `/travel/:id` | GET | 여행 상세 페이지 |
| `/api/upload` | POST | 여행 업로드 API |
| `/api/travels` | GET | 모든 여행 조회 API |
| `/api/travel/:id` | DELETE | 여행 삭제 API |
| `/api/generate-title` | POST | AI 제목 생성 API |

## 📝 API 사용 예시

### 여행 업로드
```javascript
const formData = new FormData();
formData.append('title', '부산 여행');
formData.append('description', '부산 여행 기록');
formData.append('latitude', '35.1795');
formData.append('longitude', '129.0756');
formData.append('tags', '바다, 노을');
formData.append('media', fileInput.files[0]);

fetch('/api/upload', {
    method: 'POST',
    body: formData
});
```

### AI 제목 생성
```javascript
fetch('/api/generate-title', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json'
    },
    body: JSON.stringify({
        latitude: 35.1795,
        longitude: 129.0756,
        photoCount: 12,
        currentTitle: ''
    })
});
```

### 여행 삭제
```javascript
fetch('/api/travel/1234567890', {
    method: 'DELETE'
});
```

## 🖼️ 사용자 흐름

```
1. 홈 (/)
   ↓
2. [+ 여행 추가] 클릭
   ↓
3. Step 1: 지도에서 위치 선택
   ↓
4. Step 2: 사진/영상 다중 업로드
   ↓
5. Step 3: AI가 제목 자동 생성 (또는 수동 입력)
   ↓
6. Step 4: 최종 확인 후 저장
   ↓
7. 여행 상세 페이지로 이동
```

## ⚠️ 주의사항

- `uploads/` 폴더는 자동으로 생성됩니다
- `data/travels.json`은 자동으로 생성됩니다
- 서버 재시작 후에도 데이터는 `travels.json`에 저장되어 유지됩니다
- Claude API 키 없으면 AI 제목 생성 기능 미동작

## 🐛 트러블슈팅

### "Cannot find module 'express'"
→ `npm install` 실행

### "Port 3000 is already in use"
→ 다른 프로그램이 3000 포트 사용 중. 포트 변경 또는 프로그램 종료

### "Cannot find module '@anthropic-ai/sdk'"
→ `npm install` 실행하여 의존성 설치

### "AI 제목 생성이 작동하지 않음"
→ 1. Google Gemini API 키 설정 확인
→ 2. `.env` 파일에 `GEMINI_API_KEY` 설정 확인
→ 3. API 키 유효성 확인 (Google AI Studio)
→ 4. API 키 없으면 기본 템플릿 제목으로 대체

## 📞 지원

질문이나 버그 보고는 GitHub Issues를 이용해주세요.

